#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
    const char* nombre_archivo = "archivo.txt";
    FILE *archivo = fopen(nombre_archivo, "rb");

    if(archivo == NULL){
        
    }
}